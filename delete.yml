---

- name: DELETE request
  uri:
    url: "{{ url }}"
    method: DELETE
    user: "{{ user }}"
    password: "{{ password }}"
    force_basic_auth: yes
    return_content: yes
    validate_certs: "{{ validate_certs }}"
  register: response
